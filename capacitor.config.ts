import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.hoppingstarfishflit',
  appName: 'hopping-starfish-flit',
  webDir: 'dist'
};

export default config;
